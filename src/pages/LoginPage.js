import Layout from "../Layout/Layout";
import LoginFrom from "../components/Login/Login";
const Login = () => {
  return (
    <Layout>
      <LoginFrom />
    </Layout>
  );
};

export default Login;
